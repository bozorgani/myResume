export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {}
    // Note: Next.js automatically minifies CSS in production builds
    // No need for cssnano as Next.js handles CSS optimization
  }
};
